// lib/features/reproduction/screens/register_actions_screen.dart

import 'package:flutter/material.dart';

class RegisterActionsScreen extends StatelessWidget {
  const RegisterActionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('REGISTRAR')));
  }
}
